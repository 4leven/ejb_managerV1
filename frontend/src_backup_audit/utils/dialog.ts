type DialogOptions={title:string;message?:string;value?:string;placeholder?:string;confirmText?:string;danger?:boolean;multiline?:boolean};
function openDialog(options:DialogOptions,kind:"prompt"|"confirm"|"alert"){return new Promise<string|boolean|null>((resolve)=>{const root=document.createElement("div");root.className="app-dialog-overlay";const panel=document.createElement("section");panel.className="app-dialog";panel.innerHTML=`<button class="app-dialog-close" aria-label="Cerrar">×</button><div class="app-dialog-icon">${options.danger?"!":"✦"}</div><h2></h2><p></p><div class="app-dialog-field"></div><div class="app-dialog-actions"><button class="app-dialog-cancel">Cancelar</button><button class="app-dialog-confirm">${options.confirmText??(kind==="alert"?"Entendido":"Confirmar")}</button></div>`;root.append(panel);document.body.append(root);panel.querySelector("h2")!.textContent=options.title;const p=panel.querySelector("p") as HTMLElement;p.textContent=options.message??"";p.hidden=!options.message;const field=panel.querySelector(".app-dialog-field") as HTMLElement;let input:HTMLInputElement|HTMLTextAreaElement|null=null;if(kind==="prompt"){input=document.createElement(options.multiline?"textarea":"input") as HTMLInputElement|HTMLTextAreaElement;input.value=options.value??"";input.placeholder=options.placeholder??"";field.append(input)}else field.remove();const cancel=panel.querySelector(".app-dialog-cancel") as HTMLButtonElement;if(kind==="alert")cancel.remove();const confirm=panel.querySelector(".app-dialog-confirm") as HTMLButtonElement;if(options.danger)confirm.classList.add("danger");let closed=false;const done=(value:string|boolean|null)=>{if(closed)return;closed=true;root.remove();resolve(value)};(panel.querySelector(".app-dialog-close") as HTMLButtonElement).onclick=()=>done(kind==="confirm"?false:null);if(kind!=="alert")cancel.onclick=()=>done(kind==="confirm"?false:null);confirm.onclick=()=>done(kind==="prompt"?input!.value:true);root.onclick=e=>{if(e.target===root)done(kind==="confirm"?false:null)};root.onkeydown=e=>{if(e.key==="Escape")done(kind==="confirm"?false:null);if(e.key==="Enter"&&kind!=="prompt")confirm.click()};setTimeout(()=>input?.focus(),0)});}
export const uiPrompt=(title:string,value="",options:Partial<DialogOptions>={})=>openDialog({...options,title,value},"prompt") as Promise<string|null>;
export const uiConfirm=(title:string,message="")=>openDialog({title,message,confirmText:"Sí, continuar",danger:true},"confirm") as Promise<boolean>;
export const uiAlert=(title:string,message="")=>openDialog({title,message},"alert") as Promise<boolean>;
