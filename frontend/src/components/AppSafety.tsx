import {Component,ReactNode,useEffect,useState} from "react";
export class ErrorBoundary extends Component<{children:ReactNode},{failed:boolean}>{state={failed:false};static getDerivedStateFromError(){return{failed:true}}componentDidCatch(error:unknown){console.error("EJB UI error",error)}render(){return this.state.failed?<main className="fatal-state" role="alert"><img src={`${import.meta.env.BASE_URL}ejb-manager-logo.svg`} alt="EJB Manager"/><h1>No pudimos mostrar esta sección</h1><p>Tu información permanece segura. Recarga la página para intentarlo nuevamente.</p><button onClick={()=>location.reload()}>Recargar</button></main>:this.props.children}}
export function ConnectionStatus(){const [online,setOnline]=useState(navigator.onLine);useEffect(()=>{const yes=()=>setOnline(true),no=()=>setOnline(false);addEventListener("online",yes);addEventListener("offline",no);return()=>{removeEventListener("online",yes);removeEventListener("offline",no)}},[]);return online?null:<div className="offline-banner" role="status" aria-live="polite">Sin conexión. Los cambios se enviarán cuando recuperes internet.</div>}
