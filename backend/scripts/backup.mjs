import{mkdir,writeFile}from"node:fs/promises";import{spawn}from"node:child_process";import"dotenv/config";
const directory=new URL("../backups/",import.meta.url),stamp=new Date().toISOString().replaceAll(":","-").slice(0,19),target=new URL(`ejb-${stamp}.sql`,directory);await mkdir(directory,{recursive:true});const chunks=[];const child=spawn("pg_dump",[process.env.DATABASE_URL??""],{stdio:["ignore","pipe","inherit"]});child.stdout.on("data",chunk=>chunks.push(chunk));const code=await new Promise(resolve=>child.on("close",resolve));if(code)throw new Error("pg_dump no pudo completar el respaldo");await writeFile(target,Buffer.concat(chunks));console.log(`Respaldo creado: ${target.pathname}`);
