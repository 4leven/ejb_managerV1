import"dotenv/config";const required=["DATABASE_URL","JWT_SECRET","FRONTEND_URL"],missing=required.filter(key=>!process.env[key]);if(missing.length){console.error(`Faltan variables de producción: ${missing.join(", ")}`);process.exit(1)}if(process.env.JWT_SECRET.length<32){console.error("JWT_SECRET debe tener al menos 32 caracteres");process.exit(1)}console.log("Configuración mínima de producción validada.");
