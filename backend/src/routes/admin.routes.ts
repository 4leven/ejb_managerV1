import{Router}from"express";import{bulkImport,completeOnboarding,purgeTrash,restoreTrash,trash,updatePermissions}from"../controllers/admin.controller.js";export const adminRoutes=Router().patch("/usuarios/:id/permisos",updatePermissions).patch("/onboarding",completeOnboarding).get("/papelera",trash).patch("/papelera/:id/restaurar",restoreTrash).delete("/papelera/:id",purgeTrash).post("/importar",bulkImport);
