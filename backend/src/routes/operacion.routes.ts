import{Router}from"express";import{addFlowVersion,createFlow,createPermission,createRequirement,deleteFlow,deleteRequirement,flows,permissions,requirements,updateRequirement}from"../controllers/operacion.controller.js";import{requirePageAccess}from"../middlewares/pageAccess.js";export const operacionRoutes=Router().get("/requerimientos",requirePageAccess("verRequerimientos"),requirements).post("/requerimientos",createRequirement).patch("/requerimientos/:id",updateRequirement).delete("/requerimientos/:id",deleteRequirement).get("/permisos",permissions).post("/permisos",createPermission).get("/flujos",requirePageAccess("verFlujosAreas"),flows).post("/flujos",createFlow).post("/flujos/:id/versiones",addFlowVersion).delete("/flujos/:id",deleteFlow);
