import{Router}from"express";import{archiveNotification,listNotifications,preferences,readAllNotifications,readNotification}from"../controllers/notification.controller.js";export const notificationRoutes=Router().get("/",listNotifications).patch("/leer-todas",readAllNotifications).patch("/preferencias",preferences).patch("/:id/leer",readNotification).patch("/:id/archivar",archiveNotification);
