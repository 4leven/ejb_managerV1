import { spaceActivity, removeSpace } from "../controllers/chat-space.controller.js";
import{Router}from"express";import{actualizar,crear,firmar,guardarMiFirma,invitar,listar,listarInvitaciones,obtenerMiFirma,rechazar,responderInvitacion}from"../controllers/colaboracion.controller.js";export const colaboracionRoutes=Router().get("/invitaciones/grupos",listarInvitaciones).post("/grupo/:id/invitaciones",invitar).post("/grupo/:id/invitaciones/:invitacionId/responder",responderInvitacion).get("/firma/perfil/mi-firma",obtenerMiFirma).put("/firma/perfil/mi-firma",guardarMiFirma).post("/firma/:id/firmar",firmar).post("/firma/:id/rechazar",rechazar).get("/:tipo",listar).post("/:tipo",crear).post("/:tipo/:id/actividad",spaceActivity).delete("/:tipo/:id",removeSpace).patch("/:tipo/:id",actualizar);
