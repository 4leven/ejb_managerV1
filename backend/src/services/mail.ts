export async function sendMail(to:string,subject:string,html:string){const webhook=process.env.EMAIL_WEBHOOK_URL;if(webhook){const response=await fetch(webhook,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({to,subject,html})});if(!response.ok)throw new Error("El servicio de correo no respondió correctamente");return}if(process.env.NODE_ENV!=="production")console.info(JSON.stringify({level:"info",event:"email-preview",to,subject,html}))}
